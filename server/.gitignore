node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Transient oracle output from generate-hidden-testcases.ts — reviewed by
# hand, then merged into problems/<slug>/tests.json; not meant to be committed.
problems/*/tests.generated.json
