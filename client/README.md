# AlgoMinds — client

React 19 + Vite frontend for AlgoMinds. See the [root README](../README.md) for full setup (Docker infra, `.env.local`) and [`CLAUDE.md`](../CLAUDE.md) for architecture (feature-folder structure, state management conventions).

## Quick reference

```bash
npm install
npm run dev
```

```bash
npm run build   # tsc -b && vite build
npm run lint
npm run preview
npm run test
```
